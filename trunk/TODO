* move tools.hh from directory folder into directory tools (Claus)
* place the logic of when a protein is stably folded into folder classes (Claus)
* separate generic functions from organism-specific functions in GenebankAnalyzer.
* break up FitnessEvaluator hierarchy (a singe FE base class doesn't make sense
  anymore with the new Population class).
* make function ErrorproneTranslation::getCodonCosts() not return a pointer
  to an internal data structure. (?)

